const ADD_IMAGE = Symbol();
const DELETE_IMAGE = Symbol();
const TOGGLE_IMAGE = Symbol();
const DOWNLOAD_IMAGE = Symbol();

const actionTypes = {
  ADD_IMAGE,
  DELETE_IMAGE,
  TOGGLE_IMAGE,
  DOWNLOAD_IMAGE
};

export default actionTypes;
